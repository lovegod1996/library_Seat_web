<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xoqao.web.dao.NewsMapper">
    <!--添加资讯-->
    <insert id="insertNews">
        <selectKey resultType="java.lang.Integer" keyProperty="nid" order="AFTER" >
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO news (title,content,creattime) VALUES (#{news.title},#{news.content},#{news.creattime})
    </insert>

    <!--查看所有资讯-->
    <select id="findAllNews" resultType="com.xoqao.web.bean.news.News">
        SELECT nid,title,content,creattime FROM  news
        ORDER BY  creattime DESC
    </select>

    <select id="findAllNewsPage" resultType="com.xoqao.web.bean.news.News">
        SELECT nid,title,content,creattime FROM  news
        ORDER BY  creattime DESC
         limit #{startRow},#{pageSize}
    </select>

    <!--查看详细资讯-->
    <select id="findNewsByid" resultType="com.xoqao.web.bean.news.News">
         SELECT nid,title,content,creattime FROM  news WHERE nid =#{nid}
    </select>

    <!--删除资讯-->
    <delete id="deleteNews" >
        DELETE FROM news WHERE  nid =#{nid}
    </delete>
</mapper>